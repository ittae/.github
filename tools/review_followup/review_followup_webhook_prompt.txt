GitHub PR follow-up webhook event received.

Event name: {event}
Event action: {action}
Repository: {repository.full_name}
PR number: {pull_request.number}
PR title: {pull_request.title}
PR URL: {pull_request.html_url}
Issue URL fallback: {issue.html_url}
PR merged: {pull_request.merged}
Merge commit: {pull_request.merge_commit_sha}
Merged at: {pull_request.merged_at}
Merged by: {pull_request.merged_by.login}
Sender: {sender.login}
Review author: {review.user.login}
Review id: {review.id}
Review URL: {review.html_url}
Comment author: {comment.user.login}
Comment id: {comment.id}
Comment URL: {comment.html_url}
Review body: {review.body}
Comment body: {comment.body}
Issue body: {issue.body}

Task:
1. Determine the canonical PR URL. Prefer `{pull_request.html_url}`. If that is empty and `{issue.html_url}` is a pull request conversation URL, use that instead.
2. If `{action}` is `closed` and `{pull_request.merged}` resolves to `true`, treat this as merge aftercare instead of review-gate follow-up:
   - Run `python3.11 {{REVIEW_FOLLOWUP_PATH}} --merged-aftercare-pr-url <PR_URL> --statuses done,in_review,in_progress,blocked,todo,backlog --reviewer-roster-file {{REVIEWER_ROSTER_PATH}} --create-triage-issue-on-miss --fallback-project-title {{FALLBACK_PROJECT_TITLE}} --event-action "{action}" --sender "{sender.login}" --head-sha "{pull_request.head.sha}" --head-ref "{pull_request.head.ref}" --base-ref "{pull_request.base.ref}" --pr-merged "{pull_request.merged}" --merge-commit-sha "{pull_request.merge_commit_sha}" --merged-at "{pull_request.merged_at}" --merged-by "{pull_request.merged_by.login}" --apply-aftercare --output json`.
   - Read `resolution.state`, `matches`, `created_issue`, `aftercare.state`, `aftercare.updated_issues`, `aftercare.posted_comments`, and `warnings`.
   - If `created_issue` exists, stop there. The resolver already left a visible Multica tracking issue.
   - If `aftercare.state` is `completed` or `noop`, return a concise summary and do not post an extra Multica comment elsewhere.
   - If `aftercare.state` is `blocked`, return `blocked` with the ambiguous families or warnings instead of guessing.
   - Do not run the review-gate flow below for merged PRs.
3. If this is an `issue_comment` payload without a pull request URL, stop and return `blocked` with the reason `non-pr-issue-comment`.
   - Note: `pull_request` events with action `edited` or `renamed` (PR title/body changes) are valid link-correction triggers, even with no new commit, review, or `synchronize`. Do not skip them; continue through the resolve and gate steps so a newly added `ITT-123` reference is detected and recorded.
4. Run `python3.11 {{REVIEW_FOLLOWUP_PATH}} --resolve-pr-url <PR_URL> --statuses in_review,in_progress,blocked,todo --reviewer-roster-file {{REVIEWER_ROSTER_PATH}} --create-triage-issue-on-miss --fallback-project-title {{FALLBACK_PROJECT_TITLE}} --event-name "{event}" --event-action "{action}" --sender "{sender.login}" --review-author "{review.user.login}" --review-id "{review.id}" --review-state "{review.state}" --review-body "{review.body}" --review-url "{review.html_url}" --comment-author "{comment.user.login}" --comment-id "{comment.id}" --comment-body "{comment.body}" --comment-url "{comment.html_url}" --head-sha "{pull_request.head.sha}" --output json`.
5. Read `resolution.state`, `matches`, `created_issue`, `triage_preview`, and `warnings`.
6. If `resolution.state == needs-triage` and `created_issue` exists, stop there. The resolver already left a visible Multica tracking issue, so do not post an extra comment elsewhere.
7. If `resolution.state == ignored-external-repo`, stop there and return `external-repo-skipped`. Unlinked fallback tracking applies only to `owner == ittae` repositories.
8. If more than one linked Multica issue matches the same PR, return `blocked` and list the candidates instead of guessing.
9. If exactly one linked issue exists, run `python3.11 {{REVIEW_FOLLOWUP_PATH}} <ISSUE_IDENTIFIER> --pr-url <PR_URL> --reviewer-roster-file {{REVIEWER_ROSTER_PATH}} --event-name "{event}" --event-action "{action}" --sender "{sender.login}" --review-author "{review.user.login}" --review-id "{review.id}" --review-state "{review.state}" --review-body "{review.body}" --review-url "{review.html_url}" --comment-author "{comment.user.login}" --comment-id "{comment.id}" --comment-body "{comment.body}" --comment-url "{comment.html_url}" --head-sha "{pull_request.head.sha}" --stabilize-seconds 5 --stabilize-attempts 2 --output json`.
10. Read `follow_up`, `apply_plan`, and especially `notification.should_post`, `notification.mode`, `notification.parent_comment_id`, `notification.comment_body`, `notification.suppression_reason`, `notification.dedupe_key`, `notification.ci_state`, and `notification.verdict`. Also read `link_established_notification.should_post`, `link_established_notification.comment_body`, `link_established_notification.suppression_reason`, and `link_established_notification.dedupe_key` (only populated for `pull_request` `edited`/`renamed` events that newly establish or confirm a Multica link).
11. The script owns gate semantics. Do not infer reviewer roster state, dedupe keys, or correction policy from the issue timeline yourself when `notification` is present.
12. If `notification.should_post == false`, the gate comment is internal-only (same-head repeated `collecting_reviews`, duplicate semantic state, or duplicate event-key delivery). Skip posting the gate comment, but still evaluate `link_established_notification` in step 14 before returning. Only return `internal-only` if both `notification.should_post` and `link_established_notification.should_post` are false.
13. If `notification.should_post == true`, post exactly `notification.comment_body` to the linked issue.
   - When `notification.mode == reply`, post it as a reply with `--parent <notification.parent_comment_id>`.
   - Otherwise post it as a top-level comment.
   - In agent/webhook contexts, prefer `python3 {{MULTICA_GUARD_PATH}} comment-add <ISSUE_IDENTIFIER> --content-stdin` over raw `multica issue comment add` when `MULTICA_AGENT_ID`, `MULTICA_TASK_ID`, and `MULTICA_TOKEN` are present.
   - Do not rewrite, summarize, or embellish the generated comment text. The hidden metadata block inside the body is required for future dedupe/correction.
14. If `link_established_notification.should_post == true`, also post exactly `link_established_notification.comment_body` as a top-level comment on the linked issue. This is the visible record that a PR title/body edit established (or confirmed) the Multica link; it carries the PR URL, head SHA, review/CI state, the 반영/보류/기각/미처리 disposition summary, and whether a Hermes synthesis verdict is still needed. If `link_established_notification.should_post == false`, do not post it (the snapshot was already recorded for this head). Use the same posting path and no-rewrite rule as step 13. Posting this comment is independent of `notification.should_post`.
15. If `follow_up.state == needs_agent_fix` and any `apply_plan` entry uses `create-follow-up-issue`, you may add one short sentence after posting that a follow-up issue proposal is needed, but do not create the child issue unless the issue explicitly authorizes automation.
16. Do not post GitHub comments. Do not merge. Do not push. Do not change branch protection. Without explicit user approval, do not trigger code changes or auto-merge.
